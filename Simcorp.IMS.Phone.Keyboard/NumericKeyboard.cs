namespace Simcorp.IMS.Phone.Keyboard {
    public class NumericKeyboard : BaseKeyboard {
        public override string ToString() {
            return "Keyboard with numeric buttons";
        }
    }
}

